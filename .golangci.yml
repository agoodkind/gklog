version: "2"

# Extends the shared agoodkind golangci config (v2 schema).
# Add project specific overrides below.
extends:
  - https://raw.githubusercontent.com/agoodkind/go-makefile/main/golangci-template.yml
